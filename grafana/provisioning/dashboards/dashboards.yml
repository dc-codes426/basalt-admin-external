apiVersion: 1

providers:
  - name: dev
    orgId: 1
    folder: "Dev"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/dev

  - name: prod
    orgId: 1
    folder: "Prod"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/prod
